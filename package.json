{
  "name": "@tallman5/cut-sheet-generator",
  "version": "1.0.0",
  "author": "Joseph McGurkin <joseph@mcgurkin.net>",
  "description": "Generate cut sheets for given lists of needed pieces and stock.",
  "main": "dist/index.js",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "dist": "tsc --project tsconfig.build.json",
    "distpack": "tsc --project tsconfig.build.json && npm pack",
    "prepublishOnly": "npm run build",
    "publish": "npm publish --access=public",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "canvas": "^3.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.13.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.6",
    "typescript": "^5.8.2"
  }
}
